# 开发与入库记录

## 目标

构建可复用的 Chrome ChatGPT 网页桥接，支持三个 tab 并发生图、模型选择、提示词提交、快速状态查询和对应原图获取。对 AI 提供短请求 MCP，任务状态由本机服务持久化。

## 依赖与安装

- @modelcontextprotocol/sdk 1.30.0：MCP 服务。
- ws 8.21.3：本机扩展 WebSocket 连接。
- chrome-devtools-mcp 1.9.0：前期调研时安装的项目本地依赖，当前桥接运行不依赖它。
- jsdom 29.0.2：测试依赖。曾试用 30.0.1，因与当时 Node 24.13.0 不兼容，替换为当前版本。
- ws 最初使用 8.18.3，开发期间升级为 8.21.3。
- npm 安装使用 --ignore-scripts --no-audit --no-fund；传递依赖锁定于 package-lock.json。
- Chrome 扩展由 npm run setup 在本机生成；MCP 由 scripts/install-mcp.ps1 注册；Skill 源码位于 skill/SKILL.md。
- 使用现有 Node 和 Python 环境。没有创建开机计划任务。

## 实现

- src/config.mjs、setup.mjs：本地认证配置、稳定扩展 ID、生成安装包。
- src/store.mjs：tab 身份、持久化请求去重、模型和草稿前置检查、状态过期、结果关联。临时 WEB 聊天 URL 只能在用户消息和文档身份吻合时转为正式聊天身份。
- src/service.mjs：认证的 localhost HTTP 和扩展 WebSocket、并行观测、命令超时、逐 tab 操作锁和逐 profile 下载锁。
- src/daemon.mjs、client.mjs、cli.mjs：后台服务、按需启动和 UTF-8 JSON 参数入口。
- src/mcp.mjs：九个 MCP 工具，返回精简状态及 structuredContent。
- src/downloads.mjs：对应浏览器图片与下载文件的 SHA-256 比对、逐 run 归档，避免错误关联或覆盖不同内容。
- extension/adapter.js：当前页面模型菜单、编辑器、状态、回答、图片查看器和保存按钮适配；版本 15。
- extension/content.js、background.js：页面观察、身份校验、命令收据、断线重连及有限下载事件候选。
- extension/popup.*：连接与 tab 状态展示。
- test/：31 个行为测试；scripts/：安装、诊断、实机证据、查询性能、图片解码和重启检查。

## 真实页面适配

实测修复了 composer 中的嵌套模型菜单与 PointerEvent 交互、aria-checked 读回、aria-labelledby、隐藏菜单过滤、assistant 同级图片区、中文“复制回复”和查看器“保存”控件。

下载等待会复用匹配的查看器，允许首次界面加载耗时，并只操作包含目标图片的查看器。普通聊天提交不会误用图片编辑框；查看器有草稿时保留草稿。移除了调试用大块 HTML 快照，日常状态保持精简。

缺少 referrer 的 Chrome 下载事件不能独立确认归属，保留为未知收据；下载文件的确切字节与对应浏览器图片一致才标为已验证。没有构造 ChatGPT 私有接口请求，也未提取浏览器会话凭证。

## 验收

真实三 tab 生图、GPT-5.5 网页选择、幂等提交、五 tab 状态查询、对应回答、原图保存解码和本地服务重启恢复均已测试。性能、范围与限制见 ACCEPTANCE.md。原始证据包含私人聊天和本机身份，仅保留本地，未放入版本控制。

## 仓库整理 — 2026-09-09

- 将源码纳入 ReduckProject/chrome-extention-hub 的 chatgpt-web-bridge/ 独立子目录，共用主仓库 Git 历史。
- 新增仓库 README、.gitignore、.gitattributes；不使用 Git submodule。
- 复制实现、扩展、安装和验证脚本、测试、Skill 及依赖锁定文件；运行核心源码保持原样。
- 将安装说明改为相对子项目目录和可配置绝对路径；将本机验收文档整理为可入库摘要。
- 本机 runtime、认证配置、聊天记录、图片、日志和 node_modules 不提交。当前已安装实例继续使用原运行配置，本次入库不自动迁移 Chrome 已加载目录。
- 在新目录通过 npm ci 安装锁定依赖，运行既有测试，检查将提交的文件，再建立 main 分支初始提交并关联 origin。

## 重新加载后的测试与图片加载修补 — 2026-09-09

- 用户确认重新加载扩展，要求验证切模型、新聊天、生图、进度和下载。以新连接创建独立空白聊天，通过实际 MCP SDK/stdio 执行全部步骤。
- GPT-5.6 Sol 和 GPT-5.5 两次切换均确认选中；发送一个相机测试图，观察 generating、finalizing 和 completed，最终下载、哈希匹配、解码和查看均通过。
- 诊断记录显示图片在后台 tab 中为 loading:lazy、loadState:pending、naturalWidth/Height:0，但回答结束控件已出现。这会阻止原先的完成条件。
- extension/adapter.js 版本 18 增加 loadState/loading 观测，并在最新回答已结束时启动尚未加载的同源 lazy 图片。旧回答、外域图片和仍在生成中的图片不会被该处理改动；不把 pending 图片伪装成 loaded。
- 新增一项对应的行为回归测试。使用 setAttribute 更新 loading，兼容测试 DOM 与真实浏览器的属性反映差异；32/32 测试通过。
- npm run setup 更新安装目录，refresh_observers 更新现有页面，无需再次让用户重新加载整个扩展。没有更改 MCP/Skill 的用户级注册位置。
- 更详细的本次测试数据和生成图仍只保留在本机 artifacts，Git 中记录可公开的结果摘要。整体等待含诊断过程，不用于声称生图性能改善。

## 后台回答完成通知延迟修复 — 2026-09-10

- 回归实测发现：回答流已结束，但后台页面未出现可见结束按钮，任务在 finalizing 多停留约 88 秒。先前移除图片 loaded 条件仍未解除控件渲染依赖。
- adapter v33 使用 PerformanceObserver 和已有 Resource Timing，被动保存最多 32 个响应流结束记录；只识别同源响应流，不包装 fetch、不复制响应正文、不清理网站缓冲、不请求会话列表。
- 服务在本次提交、同一文档及用户／助手消息归属确认后，结合成功响应流结束、页面空闲和 2.5 秒稳定期完成任务。completionEvidence 记录响应流或控件证据；不能用旧流、失败流、不同文档、忙碌或过期观察完成当前任务。
- 为正在运行的任务补充有界 DOM 探测，最多 4 个并发、同一 tab 至少 3 秒间隔、超时 2 秒，失败逐步退避；结束／访问暂停后停止。查询不会激活页面或加载历史／图片。
- 显式 loadImages 传递已完成回答的消息身份，避免再次等待图片按钮。页面仍检查空闲、当前用户／助手消息和访问暂停；默认 result 保持纯读取。
- 71 项测试通过，覆盖无按钮和未加载媒体的通用回答完成、缓冲被清理、消息隔离、后台心跳停滞、探测并发／退避，以及完成后的显式加载。
- 更新本机安装目录并重启本地服务，现有五个页面保持原 documentId 热更新至 v33。新测试在相同 tab／requestId 下等待页面就绪，实际仅提交一次；提交和完成时均为后台页面，没有使用 CUA 激活页面。
- 新实测回答流耗时约 32.7 秒，流结束后约 3.8 秒进入 completed；当时 finalActions:false、图片 lazy/pending。没有把原图加载或保存当成回答完成。首次读取新首页模型菜单未识别，测试保留网页默认设置，未声称确认具体模型。
- 此次窗口未发现 HTTP 429。会话列表由网页产生的跨 tab 刷新仍需独立归因，本修复不声称已解决该限流根因。详细记录保留于本地被忽略的 artifacts/completion-fix-20260910。

## 多 tab 限流复发与模型入口适配 — 2026-09-10

- 本次保存的历史列表请求共 28 次、HTTP 429 共 5 次；一次发送后 6 个 tab 在约 26 毫秒内同步请求列表。用户随后报告手动操作也触发限制。这支持多 tab 请求放大的判断，现有时序不含调用栈，不能确定网页内部触发链。
- 用户选择串行复用，不设置总 tab 数硬上限，也不自动关闭用户页面。更新源码和用户级 bridge Skill、用户级 gen-image Skill，跨批次仍优先复用本任务已完成且结果已保存的 tab。保留 @创建图片 前缀等其他会话的约定。
- 实际 DOM 显示部分模型入口只写“中”或“高”；adapter v41 识别这类 composer 控件，分别返回 name、reasoningEffort 和原始 label。模型名称从明确版本文本或已勾选模型菜单获得，缓存标记 nameIsCached/nameSource/nameObservedAt；不能根据强度推测型号。弹窗遮挡时读取控件值并标记不可见，不点击。
- 新文档上报不再广播所有 tab 的观察器重装；连接建立及显式 refresh_observers 保留。新聊天临时打开侧栏后尝试恢复折叠，保留原来展开的用户侧栏。MCP 返回 closed/frozen/discarded。
- 合并保留另一会话未提交的 v35 图片 section 识别、占位替换和目标任务 wait 隔离修复。完整测试 83 项通过，最后的强度组合标签修改重跑 32 项 adapter 测试通过；三份 Skill 通过验证。
- 备份状态后仅重启本项目 daemon，保留 54 个任务和访问暂停；四个现有 tab 热更新至 v41，安装文件与源码一致。真实 stdio MCP 返回 12 个工具，并读回新字段。当前页面只显示强度，模型名称仍为 null，实际菜单读取／切换尚未在访问恢复后验收。
- 现场日志、部署前后快照和 MCP 验证位于被忽略的 artifacts/recurrence-2026-09-09T18-27-39-023Z。

## “最新”模型识别与新页面启动版本修复 — 2026-09-10

- 用户反馈滚动“最新”只显示推理强度，而固定 5.5／5.6 显示型号前缀。实机模型菜单确认三个有效选项为“最新”、GPT-5.6 Sol、GPT-5.5；此前名称提取只接受具体型号，错误过滤了“最新”。
- adapter v42 接受“最新”／Latest 作为模型选项名称。已确认的 composer pill 布局仅显示强度或模式时返回 name:最新／Latest、nameSource:latest_selector；不推测其具体后端型号。name、reasoningEffort、nameSource、nameIsCached 和 nameObservedAt 分别保留，菜单读取及切换可以验证“最新”。
- 只新建一个空白诊断 tab，首次记录到 adapterVersion:15、模型未识别；此时已有页面为热更新的 v41。说明 Chrome 缓存的 manifest 静态启动脚本仍在新文档加载旧 adapter，显式热更新现有页面不能覆盖新页首次启动问题。
- 扩展版本升至 0.1.1，manifest 只注册 content.js；contentVersion:4 首次上报前请求后台给当前 tab 加载固定 adapter.js。加载消息校验本扩展、ChatGPT 顶层页面并锁定发送者 tab，不接受自定义脚本或目标，不触发其他 tab 重装。加载失败不发布旧快照，允许显式重试；重复启动不新增观察器。MCP 同时返回 contentVersion。
- 完整测试 86 项通过；源码及用户级 bridge Skill、用户级 gen-image Skill 通过验证。npm run setup 已更新原安装目录；现有诊断页热更新到 v42 后，真实执行“最新 → GPT-5.5 → GPT-5.6 Sol → 最新”，全部 confirmed:true，模型及推理强度均读回成功，没有发送提示词或生成图片。
- 新启动脚本首次启用需要重新加载 Chrome 扩展。自动化访问扩展管理页被浏览器 URL 安全策略拒绝，已请用户手动重载；后续须在不先热更新的条件下记录新文档首次 adapterVersion:42 / contentVersion:4。重载前不宣称新文档启动已通过实机验证。
- 现场证据保留在被忽略的 artifacts/latest-model-2026-09-09T19-10-32-913Z。

## 扩展已连接但新页面未上报的修复 — 2026-09-11

- 用户反馈另一任务把网页生图阻塞归因为“连接断开且缓存过期”。实查服务和扩展 WebSocket 正常，但状态接口只有旧浏览器会话记录；Chrome 已有新的 ChatGPT tab，却未成功启动观察器。配置仍指向当前仓库，扩展仍启用且保有 ChatGPT 主机权限。
- 原服务丢弃未曾上报页面的 invalidate 信息；status refresh 只遍历历史状态，向已关闭 tab 发 probe，无法诊断新 tab。新增 connections，保留当前浏览器会话、当前 tab、尚未上报的 tab、观察错误和新鲜页面数。默认刷新根据当前清单最多并发四个被动 probe，跳过已关闭历史和断连旧 profile；新页面后续的首份快照会加入清单，显式 tabKey 保持原有定向查询。
- 先部署诊断，收到新页面的 Receiving end does not exist。为 content 初始化失败补充 bootstrapError 后，确认后台没有确认 load_adapter 请求；原 content v4 静默退出，没有注册接收器，导致查询方将初始化失败误认为桥接断开。没有把浏览器 UI 工具的独立超时或 Promise 等待假设当作根因。
- content v5 在显式注入已预载当前 adapter 时直接启动，不再依赖旧后台确认新协议；加载失败发布不可提交的诊断快照，不读旧 adapter 冒充有效页面。更新安装包为 0.1.2，现有两个 ChatGPT 页面经热更新恢复 v42 / content v5、idle、composerReady:true、stale:false，模型名称“最新”、推理强度“高”。
- 更新源 Skill 和用户级 bridge/gen-image Skill：按连接元数据与当前页面判断，页面观察错误不自动归因为断连或要求重新登录。保留串行复用、明确限流恢复、原图验证与 @创建图片 前缀约定。
- 用户手动重载扩展后，仅新建一个空白诊断 tab，在没有先热更新的条件下首次收到 v42 / content v5，随后读回新鲜 idle 和实际勾选模型“最新 / 高”。没有提交提示词或生成图片；UI 工具 getTab 仍超时，保留这一空白测试页供用户关闭，未操作其他用户页面。
- 最终 90 项完整测试通过，包括新快照加入当前清单、定向旧记录错误不污染当前连接等回归。备份状态后仅重启本项目 daemon；实机记录在被忽略的 artifacts/connection-20260911-1789058335709。

## Skill 的窗口复用、等待与清理规则 — 2026-09-11

- 用户要求按来源清理窗口，并将最终复用阈值指定为大于等于 4 个。同步修改仓库 bridge Skill、用户级 bridge Skill 和用户级 gen-image Skill，以及 README 当前流程；未改动 MCP 或扩展代码。
- 窗口在本流程中指目标 Chrome profile 内的 ChatGPT tab，跨浏览器窗口计数。达到 4 个后只复用空闲页；没有空闲页则每 20 秒检查一次，初次检查后最多 5 次，约 100 秒后仍达到 4 个且无空闲页即中断并汇报。低于阈值才可按需逐个新建，并行任务也先检查数量。
- 记录 openedByThisTask，整个任务及必要结果保存／归档完成后关闭本任务新建的 tab，复用的保留；在同一 tab 新建聊天不改变来源，多张图片中途不反复关页新开。关闭前核对完整身份及无草稿、活动任务，关闭后核验；工具不支持或失败时报告遗留页面。
- 保留精确提示词与 @创建图片 前缀、模型和推理强度识别、原图校验及限流处理约定。当前 MCP 尚无关闭 tab 接口，Skill 按实际可用的浏览器关闭能力执行，不虚构工具或关闭成功。三份 Skill 完成验证；本次为文档规则修改，不运行生图或浏览器关闭操作。

## 跨客户端任务占用、统一队列与提交节奏 — 2026-09-11

- 新现场只有两个当前 tab 仍发生会话列表 429：一个 tab 回答流结束约 66 ms 后，另一 tab 读取列表并随后收到 429。审计还发现两个客户端在同一 tab 上交错 new_chat、models 和 send。时序支持跨页历史更新的推断，不含网站请求发起栈，不宣称已证明网页内部根因。
- 服务和 MCP 升级至 0.2.0，新增 chatgpt_task。每项任务以唯一 taskId 申请整个 profile 的占用，取得 leaseId 后才可操作页面，首次动作绑定一个 tab。占用跨越新聊天、模型选择、提交、下载和调用方归档，completed 不自动释放；调用方确认结果处理后显式 release。旧客户端缺少凭据时被服务端拒绝，CLI 使用同一校验。
- 同 profile 共享 FIFO 队列，默认每 20 秒检查，初次申请之后第 5 次仍不可用则 timed_out；提前查询不抢先获取或消耗次数。无人继续检查的队列项过期清理；active 占用不会超时自动转交。队列不自动发送提示词，服务重启保留占用、队列和发送时间。
- 默认两次提交至少相隔 120 秒，回答完成后再留 30 秒；在新聊天、模型操作和发送之前拒绝过快操作，PROFILE_COOLDOWN 附 retryAfterMs，不为被拦截的新消息保留 run 或下发页面命令。这是可配置的本地保护节奏，不是网站额度承诺。相同 requestId 的幂等重查仍可用。
- 服务端按当前完整清单强制执行四个 tab 时复用、一次只新建一页；创建结果不确定时持久化标记，禁止再开，用已有清单和唯一新增页确认绑定。未新增关闭 API；任务按来源使用实际可用的关闭工具清理，复用页保留。
- 原任务可认领升级前未纳管的 run，继续原记录而不重发；仅在用户明确取消时可 abandon 自己的占用，不清草稿、不停止网页、不更改未知 run 的结论，仍在生成的页面继续阻止新工作。正常释放拒绝未确认生成、草稿、创建结果和过期观察。
- 更新仓库 Skill、任务协议参考、README 和用户级 bridge/gen-image Skills，保留各自路径约定、@创建图片 前缀和原图验证要求。旧会话未加载新 schema 时按 Skill 使用同一服务 CLI，不需要重载 Chrome 扩展；扩展仍为 0.1.2 / adapter 42 / content 5。
- 完整 100 项测试通过，三份 Skill 验证通过。实机发现另一客户端在排查期间已解除暂停并继续生成，等待当轮回答结束且无在途动作后，备份并仅重启本项目 daemon；87 条 run 全部保留，当前一页重新连接且观察新鲜。没有恢复访问或提交测试提示词。
- 实机错误 lease 的 models 请求返回 TASK_LEASE_REQUIRED，审计 browserCommands 为 0。新 stdio MCP 读取到 13 个工具及当前连接。现场与部署记录保存在 Git 忽略的 artifacts/scheduler-fix-20260911；本次只验证本地调度保护，未用新一轮生成声称网站限流已消失。

## 提交节奏调整为 10 秒 — 2026-09-11

- 按用户要求，将默认两次发送间隔和回答结束后的等待都改为 10000 ms。两个截止时间取较晚者，不串行叠加为 20 秒；队列的 20 秒／最多 5 次等待、四个 tab 阈值及任务占用规则保持原值。
- 同步 README、源 Skill／协议、用户级 bridge 与 gen-image Skill，以及本机 scheduling 两个配置项。配置修改保留其它设置；历史记录、任务占用和访问暂停状态不重置。
- 调度与服务的 30 项测试通过，包含完成后 9999 ms 仍等待、10000 ms 准时放行，以及冷却时间的重启持久化；三份 Skill 验证通过。无活动生成和在途动作时备份状态、重启本项目服务，不提交测试提示词。

## 将任务占用修正为单个 tab — 2026-09-11

- 用户确认不同任务应在不同 tab 并行，原 0.2.0 的整个 profile 串行锁扩大了范围。服务与 MCP 升级到 0.2.1 / schedulerVersion:2，任务保留独立 lease 并独占单页到结果保存、归档与清理结束。A 正在输出或保存时，B 可在其它页面工作；同页和同聊天仍不能被抢占。
- 分配与状态落盘只使用短暂的 profile 锁，网页命令按各自任务等待；慢页面命令不会占住其它任务，release 会等待本任务在途命令结束。发送意图与共享 10 秒间隔原子校验，两个客户端同时发送只能有一个创建新 run；另一个按剩余间隔重试，无需等首个回答结束。
- 回答结束后的 10 秒等待只作用于对应 tab 的下一轮新聊天、模型操作和发送；与发送间隔取较晚截止时间，不相加。accessPause 仍按网站访问限制暂停整个 profile，不因本次并发修正自动恢复。
- 新页名额与当前完整清单共同计数，少于 4 个时可以预留一页，达到 4 个时原子认领空闲页，只有无空闲页才每 20 秒检查、初次之后最多 5 次。未知清单不创建页面；并发预留不能越过阈值，待确认创建的页面不会被另一任务抢走。状态通过 lockScope:tab、activeTasks、reservedTabCount 和 allocation 明示范围与归属。
- 升级保留原 taskId、leaseId、队列、run、requestId、发送时间及暂停记录，移除旧 activeTaskId 的 profile 独占指针。同步 README、源 Skill/协议以及用户级 bridge/gen-image Skill，保留精确提示词、模型与强度、原图验证、按来源清理和明确恢复规则。
- 完整 105 项本地测试通过，覆盖跨 tab 并行、保存期间防抢占、慢 RPC 隔离、竞争发送、竞争最后一个名额、创建确认前的保护、五次等待及持久化迁移。三份 Skill 验证通过；新 stdio MCP 返回 13 个工具并可读取实际 Chrome 状态。
- 无在途页面动作时备份并重启本项目 daemon，99 条旧 run、请求去重记录和原活动 lease 全部保留，Chrome 重新连接且观测新鲜。扩展仍为 0.1.2 / adapter 42 / content 5，无需重新加载；没有提交新的测试提示词或停止网页生成。
- 现有网页任务再次出现会话访问频繁提示；最终重启前备份已含该暂停，升级后原样保留，未自动恢复。被动诊断保存了列表 429 与已有 RPC 时序，未把本次占用修复当作网站已解限或根因已排除。部署、验证和诊断记录保存在忽略目录 artifacts/tab-leases-1789066689613。

## 限流后等待恢复并继续整个任务 — 2026-09-11

- 按用户要求，将访问频繁处理从手动恢复改为等待 5 分钟自动检查。服务端 0.2.2 / accessRecoveryVersion:1 到期读取当前页面，必要时只确认已识别限流提示的“明白了/OK”等按钮，再检查观察状态；仍受限或检查失败则安排下一个 5 分钟，不每个 tick 重试。下一次截止时间在页面命令前持久化，重启不绕过等待。
- 限流动作通过 HTTP 200 返回 state:waiting_for_access、taskContinues:true、nextAction 和原步骤标识，MCP 不标 isError、CLI 不因限流失败退出。执行任务的 AI 持续用 access.wait 有界等待（最长 25 秒），恢复后继续原步骤与剩余提示词，不结束批次、不要求用户回复恢复。恢复不盲目重放已提交或结果不确定的请求。
- accessPause 返回 autoResume:true / resumeRequired:false，rate_limited 表示运行中的限流等待；普通 run wait 不再因限流立即返回造成循环。access.wait 可在尚无 run 或 run 已完成时等待恢复；心跳和其它 profile 不使其频繁唤醒。
- 限流等待不消耗空闲 tab 的轮询次数，恢复时续接 queued 任务有效时间，避免超过 120 秒就过期；原 task lease 与批次位置保留。10 秒发送间隔、单 tab 独占、不同 tab 并行和四个 tab 复用阈值保持原规则。
- adapter 43 新增固定 dismiss_rate_limit 操作，走已有 read 协议；只点击限流提示中的唯一确认按钮，不处理其它登录框、正文中的指令或重试按钮，不改草稿或触发发送。没有变更 manifest、background、content 协议，通过更新原安装目录 adapter 并热更新观察器应用，无需用户重载扩展。
- 同步 MCP 描述、README、源 Skill/任务协议、新的限流恢复参考，以及用户级 bridge/gen-image Skill。重点规定限流不结束任务、不得丢弃后续提示词，恢复后必须继续；保留明确失败、登录需求和未知提交的独立处理规则。
- 完整 111 项测试通过，三份 Skill 验证通过。新增回归覆盖 299999 ms 不操作、到 300000 ms 才恢复、持续限制再等 5 分钟、队列跨限流存活，以及一批两条原提示词等待后依次提交且不重复。
- 本机部署前备份，102 条 run、requestId 去重记录和活动 lease 全部保留。新 MCP 实测限流动作 isError:false / waiting_for_access，审计 browserCommands:0；access.wait 有界等待正常。原暂停截止时间后约 0.5 秒，定时器仅执行一次 DOM probe 即解除本地暂停，页面 idle、新鲜，adapter 43。未提交新的生图测试或重放旧提示词；这次页面提示此前已消失，不据此宣称网站额度已完全恢复。
- 部署与验证记录保存在忽略目录 artifacts/access-recovery-1789068585983，未把完整网页结果、提示词或安装凭据提交到仓库。
